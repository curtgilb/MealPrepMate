{
  "name": "webhook",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "npx prisma generate --schema=./schema.prisma",
    "dev": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.1.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
