import { graphql } from "@/gql";
import { FragmentType, useFragment } from "@/gql/fragment-masking";
import { Recipe } from "@/gql/graphql";

export function EditRecipeMeta(props: Recipe) {
  return <div></div>;
}
